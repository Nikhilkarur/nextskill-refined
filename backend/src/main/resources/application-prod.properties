server.port=8080

# Neon PostgreSQL connection (via env vars)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-shiny-mouse-ad37vw0x-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_l1ZJHOiPC4Go}
spring.datasource.driver-class-name=org.postgresql.Driver

# Temporarily disable Flyway due to PostgreSQL 17.6 compatibility
spring.flyway.enabled=false

# Use JPA to create tables automatically
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT secret from env var (required in production)
security.jwt.secret=${JWT_SECRET}

# CORS: override allowed origins via env var (includes wildcards for preview deploys)
security.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://*.netlify.app,https://*.vercel.app}

# H2 console disabled in prod
spring.h2.console.enabled=false

# Actuator: expose only health by default
management.endpoints.web.exposure.include=health

# JPA
spring.jpa.show-sql=false
